/**
 * Arithmetic helpers for the fixture suite.
 */

/**
 * Sums two finite operands. The caller validates finiteness at its own boundary.
 */
export function add(a: number, b: number): number {
  return a + b;
}
