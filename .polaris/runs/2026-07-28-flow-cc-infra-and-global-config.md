# Run — use more of Claude Code, improve memory, make personal-tool config global

- Date: 2026-07-28
- Task: audit Polaris against the full Claude Code feature surface (docs llms.txt), improve the memory system, and move config for run-from-anywhere commands to user level

## Timeline
- Phase 0 — discovery: dispatched researcher (Claude Code docs feature surface) and Explore (Polaris primitive-usage audit). Local finding: the user-level memory store holds 1 entry created 2026-07-15 and untouched since, against 13 daily journal files, so capture is not happening. No ~/.claude/polaris-memory/sweep or okr dirs exist yet.
- Phase 0 — evidence on the "automatic" layer: 12 of 13 journal files under ~/.claude/polaris-memory/journal/ are still `status: facts`; only 2026-07-28 is `status: narrative` (written by a manual /journal). The session-start shell skeleton lands every day; the injected "dispatch a background agent to enrich it" instruction has been honored zero times. Memory: 1 entry in 13 days. hooks/hooks.json wires SessionStart, UserPromptSubmit, SubagentStart, SubagentStop, PreToolUse, PostToolUse, and no SessionEnd, Stop, or PreCompact.
- Phase 0 — primitive audit (Explore, sonnet): returned. Confirmed live bug: /sweep is user-level since 1.8.0 but commands/journal.md:29,33,37,39 and scripts/journal-facts.sh:90 still read the deleted project-level `sweep` block and `.polaris/work/sweep-state.json`. Unused primitives: SessionEnd/Stop/PreCompact hooks, command `model` (0/29), agent `tools` (0/26), skill progressive disclosure (7 flat files, largest 60KB), skill `name` (2/7), settings.json/permissions (none shipped). Both manifests verified valid JSON.
- Phase 0 — docs research (researcher, opus): returned the verified extension surface. Decisive facts: `Stop`/`SubagentStop` accept `prompt` and `agent` handlers and honor `decision: block`; `SessionEnd` accepts neither and times out at 1.5s; `SessionStart` accepts only `command`/`mcp_tool` (which is why the injected-instruction design cannot work). `${user_config.KEY}` substitutes into skill and agent content for non-sensitive values (plugins-reference.md:576). `${CLAUDE_PLUGIN_DATA}` = ~/.claude/plugins/data/<id>/, deleted on uninstall unless --keep-data.
- Phase 1 — spec written to .polaris/specs/2026-07-28-cc-infra-memory-global-config-spec.md (self-authored rather than dispatched to product: every fact was already verified in-context). Awaiting human approval.
- Phase 1 — spec approved. Human chose full Stop scope (journal + tracker + memory) and the least-privilege pass in this run.
- Phase 3 — plan written to .polaris/plans/2026-07-28-cc-infra-memory-global-config.md, 5 slices, gate-clean. Awaiting approval.
- Phase 4 — slice 1 (stale sweep paths): fixed. journal-facts.sh reads ~/.claude/polaris-memory/sweep/state.json and emits it as its own `## Sweep` section rather than once per project; journal.md:29,33,37,39 resolve through the sweep config. Verified: no project-relative sweep refs remain, script runs exit 0.
- Phase 4 — slice 2 (Stop hook): hooks/stop-capture added and wired; session-start's two injected directives and its whole polaris_worktracker block removed. Reconcile now runs at Stop, so it covers the current session instead of the previous one, and stop-capture owns seeding .last-reconciled.local. Two bugs found and fixed while testing: control bytes and invalid UTF-8 from the snapshot made the emitted JSON unparseable; removing the session-start block left nothing to seed the tracker cursor. 10 new test cases, suite green at 79 ok / exit 0.
- Phase 4 — slice 3 (userConfig): three scalar options in plugin.json, sweep step 1 resolves user option -> JSON file -> legacy migration -> stop. notionParentPageId left not-required so a user who never runs /sweep is not forced to supply a Notion id. carryMaxDays left in the file (it already documents a default of 14). `claude plugin validate` passes.
- Phase 4 — slice 4 (least privilege): I flagged two problems first. Command `model` caps what rules/model-routing.md calls a floor, and `tools` allowlists had no evidence base because agent bodies barely name tools. Human reaffirmed both, so both shipped in full: `tools` on all 27 agents (reviewer and verifier read-only, browser MCP for e2e/tester/ui/verifier, connector MCP for product/researcher/perf/sre, Agent for audit-refactor/prod-audit/tech-writer), `model` on all 29 commands (13 opus, 16 sonnet, no haiku), allowed-tools added to gate.md, and check-agents.sh now rejects an unknown tool token (negative test included). Left `allowed-tools: Task` alone: sub-agents.md:378 documents Task as a working alias for Agent since v2.1.63.
- Phase 4 — slice 5 (docs, version): rules/memory.md documents hook-driven capture, CLAUDE.md and README list stop-capture, CHANGELOG 1.9.0 written, both manifests bumped. All changed prose gate-clean.
- Phase 5 — review dispatched on the 67-file diff.
- Phase 5 — review (reviewer, opus, read-only tools): 14 findings, 3 High. Verified the three load-bearing ones myself before fixing: `cut -c1-9500` returned 15,005 bytes on a 5-line input so the reason was never bounded; `jq --arg` already escapes control bytes and survives invalid UTF-8, so the tr/iconv pipeline defended a failure that cannot occur and the CHANGELOG claimed a bug that never existed; the withheld-snapshot branch advanced the cursor and made that window unreachable.
- Phase 5 — fixes: rewrote stop-capture. Bounded with `head -c`; every instruction now precedes the untrusted snapshot so truncation eats data, not work; the hook no longer advances the tracker cursor at all, the reconcile stamps it on completion, so an interrupted pass costs a repeated ask rather than a lost window; session id sanitized with `tr -cd` instead of a 32-bit cksum, and a missing id makes the hook silent rather than collapsing every session onto one marker; snapshot temp file removed; a first-run seed failure is reported instead of swallowed; a journal day ages out after three asks so it cannot block forever. Dropped the tr/iconv pipeline. Also: prod-audit lost Edit and NotebookEdit to match its own "reports, does not fix" description, the Asia/Kolkata userConfig default removed, worktracker-snapshot.sh's doc comment corrected, journal-facts.sh counts a sweep-only day as a day, rules/memory.md no longer overclaims that capture is required or that a silent stop means nothing was memorable.
- Phase 5 — accepted, not fixed: check-agents.sh keeps its hand-copied tool roster (a shape check catches no typo, which was the point), the 24 identical agent tool lists stay (the human chose full allowlists after I flagged that they restrict nothing), and `allowed-tools: Task` stays (a documented alias). Concurrent sessions in one project can still reconcile the same window twice, unchanged from the code this replaced.
- Phase 5 — verification: suite 88 ok, exit 0. check-agents, check-commands, and `claude plugin validate` all pass. Every changed markdown file is prose-gate clean.
- Phase 6 — QA (tester, opus): 12 findings, 1 Critical, 3 High, against an isolated HOME/TMPDIR/project. The give-up counter added in phase 5 was itself the worst of it: one malformed word in .enrich-attempts aborted the whole hook under `set -u` (F1), an unparseable count froze the counter so a day was asked forever (F2), a filename with a regex metacharacter wiped every other day's history (F3), and concurrent runs lost or double-counted increments (F5).
- Phase 6 — fixes: deleted the counter and replaced it with a stateless 14-day window, which removed four findings and 15 lines at once. Marker is now an atomic `mkdir` (8 parallel stops block once, not 8 times; a planted symlink cannot redirect the write; an unclaimable marker suppresses the block instead of blocking every turn). Unsafe session ids rejected rather than mangled. Cursor validated against an ISO pattern and reseeded when invalid or empty, so `git log --since` is never handed a date it ignores and returns the whole history for. stdin slurped so a two-object payload cannot defeat the loop-breaker. Frontmatter scan bounded to 40 lines. A failed injection screen is reported as a broken screen, not as an injection.
- Phase 6 — my own regression, caught by my own test: the rewritten awk used `exit 0` in a rule with `END{exit 1}`, and awk runs END on its way out, so END's status won and every journal file read as not-pending. The hook would have shipped completely inert, which is the exact failure it exists to fix. Fixed with a flag plus `END{exit !ok}`.
- Phase 6 — verification: suite 92 ok, exit 0. check-agents, check-commands, `claude plugin validate` pass. F1-F12 each re-tested. Residual, accepted: a symlink named YYYY-MM-DD.md inside the journal dir is still followed, and two concurrent sessions in one project can still reconcile the same window twice.
- Phase 8 — shipped as three commits on main: bf131a8 (sweep path fix), d0a1315 (Stop capture hook), 68bbb05 (global config, fleet privileges, 1.9.0). Suite green on the committed tree.
- Phase 8 — push blocked: `git push` returned 403 because the active gh account is abhishekwednesday and the repo is abhisheksharm-3/polaris. Both accounts are authenticated; not switched, because that changes global gh state another parallel session may rely on. No PR opened: the work is on main per the no-feature-branches rule.

## Outcome

Stopped at phase 8, push pending an account switch. Three commits on local main, 1.9.0, suite 92 ok. Report: .polaris/reports/2026-07-28-cc-infra-memory-global-config.md. Spend: four subagents, ~626k subagent tokens, telemetry off.
